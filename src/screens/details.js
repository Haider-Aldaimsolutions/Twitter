import { Alert, Pressable, StyleSheet, Text, View, Image } from 'react-native'
import React, { useState } from 'react'

const Details = ({ navigation }) => {

    return (
        <View><Text>Details Screen</Text></View>
    );
}
export default Details